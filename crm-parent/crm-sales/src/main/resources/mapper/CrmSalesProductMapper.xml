<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.crm.sales.mapper.CrmSalesProductMapper">

    <resultMap id="ProductResultMap" type="com.crm.sales.entity.CrmSalesProduct">
        <id column="id" property="id"/>
        <result column="category_name" property="categoryName"/>
    </resultMap>

    <select id="selectPageWithCondition" resultMap="ProductResultMap">
        SELECT p.*, pc.name AS category_name
        FROM crm_sales_product p
        LEFT JOIN crm_product_category pc ON p.category_id = pc.id AND pc.deleted_at IS NULL
        <where>
            p.deleted_at IS NULL
            <if test="keywords != null and keywords != ''">
                AND p.name LIKE '%' || #{keywords} || '%'
            </if>
            <if test="categoryId != null">AND p.category_id = #{categoryId}</if>
            <if test="status != null">AND p.status = #{status}</if>
        </where>
        ORDER BY p.created_at DESC
    </select>

</mapper>
